name: ${name}
main: ${main}
version: ${version}
author: ${author}
api-version: 1.19.4
loader: ${loader}

dependencies:
  server:
    PacketEvents:
      load: BEFORE
      required: true
      join-classpath: true
